package com.db;

import org.springframework.stereotype.Service;

@Service
public class SearchService {

	/*
	 * private SearchMapper searchMapper;
	 * 
	 * public SearchService(SearchMapper searchMapper) { this.searchMapper =
	 * searchMapper; }
	 * 
	 * public List<DTOEthanol> selectEthanol(){ return searchMapper.selectEthanol();
	 * }
	 */
}
